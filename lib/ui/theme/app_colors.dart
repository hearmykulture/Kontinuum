import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  /// Shared workout accent – matches dashboard progress rings / Start Today button.
  static const Color accentBlue = Color(0xFF448AFF);
}
